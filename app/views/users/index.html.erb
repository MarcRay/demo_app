<h1>Liste des  usagers</h1>

<table>
  <tr>
    <th>Nom</th>
    <th>Courriel</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @users.each do |user| %>
  <tr>
    <td><%= user.nom %></td>
    <td><%= user.email %></td>
    <td><%= link_to 'Montrer', user %></td>
    <td><%= link_to 'Editer', edit_user_path(user) %></td>
    <td><%= link_to 'Detruire', user, :confirm => 'Etes-vous certain ?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Nouvel  Usager', new_user_path %>
